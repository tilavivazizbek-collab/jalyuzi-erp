/**
 * Forma holati — `izoh-amal.ts` dan ALOHIDA (`use-server.test.ts`).
 */

export interface IzohHolati {
  readonly xato: string | null;
  readonly qoshildi: boolean;
}

export const BOSH_IZOH: IzohHolati = { xato: null, qoshildi: false };
